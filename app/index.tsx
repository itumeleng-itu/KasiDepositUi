import { View } from 'react-native';

import { colors } from '../src/theme';

// Placeholder until the launcher is built (phase 3).
export default function Index() {
  return <View style={{ flex: 1, backgroundColor: colors.paper }} />;
}
