import { Screen } from '../src/components/Screen';

// Built in phase 4. Exists now so setup can return here in change mode.
export default function ConfirmScreen() {
  return <Screen>{null}</Screen>;
}
