import { Screen } from '../../src/components/Screen';

// Built in phase 5. Exists now so the launcher can resume an unfinished deposit here.
export default function StatusScreen() {
  return <Screen>{null}</Screen>;
}
